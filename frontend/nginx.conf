server {
    listen 80;
    listen [::1]:80;

    location / {
        root /app;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /healthz {
        access_log off;
        default_type text/plain;
        return 200 "healthy\n";
    }
    
}
